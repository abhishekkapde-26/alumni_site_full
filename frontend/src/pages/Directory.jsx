import React,{useState} from 'react';import axios from 'axios';export default function Directory(){const [q,setQ]=useState('');const [results,setResults]=useState([]);async function search(e){e.preventDefault();const res=await axios.get(import.meta.env.VITE_API_BASE+'/api/alumni/search?q='+encodeURIComponent(q));setResults(res.data);}return(<div><h3>Directory</h3><form onSubmit={search}><input value={q} onChange={e=>setQ(e.target.value)} placeholder='Search'/><button>Search</button></form><div>{results.map(r=>(<div key={r.id}><strong>{r.name}</strong> {r.sector}</div>))}</div></div>) }